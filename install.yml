version: 1.0.1
name: statistics_calc

requirements:
  conda:
    channels:
      - conda-forge
    packages:
      - django
      - pandas
      - numpy
      - requests
      - pytz
      - scipy
      - scikit-learn
      - hydrostats
